# Terraform (M4)

Provisions cloud infra: managed Postgres, a Qdrant cluster, Redis, and a
Kubernetes cluster. API + workers ship as separate k8s Deployments with HPA.

Placeholder — modules land in M4 (Scale & infra). See docs/ARCHITECTURE.md §10.
