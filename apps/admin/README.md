# Cortex Admin UI (placeholder)

Optional, minimal Next.js admin surface (source management, process review queue,
freshness dashboards). The API-first product does not depend on it.

This is a placeholder — the actual Next.js app is scaffolded in a later milestone.
It is intentionally **excluded** from the uv (Python) workspace; see the root
`pyproject.toml` `[tool.uv.workspace] exclude`.
